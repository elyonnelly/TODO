package com.example.todo.notification

import android.content.Context
import io.reactivex.Scheduler

class NotifyWork(context: Context, params: WorkerParameters) : Scheduler.Worker(context, params) {
}